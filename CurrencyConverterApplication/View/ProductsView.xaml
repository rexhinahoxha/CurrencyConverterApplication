<UserControl x:Class="CurrencyConverterApplication.ProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CurrencyConverterApplication" xmlns:viewmodel="clr-namespace:CurrencyConverterApplication.ViewModel" d:DataContext="{d:DesignInstance Type=viewmodel:ProductsViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid x:Name="dtProducts" AutoGenerateColumns="False" ItemsSource="{Binding Products}"  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Product Name" Binding="{Binding ProductName}" Width="*" />
                <DataGridTextColumn Header="Product Price in USD" Binding="{Binding Price}" Width="*" />
                <DataGridTextColumn Header="Product Price in ..." Binding="{Binding PriceConverted}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
